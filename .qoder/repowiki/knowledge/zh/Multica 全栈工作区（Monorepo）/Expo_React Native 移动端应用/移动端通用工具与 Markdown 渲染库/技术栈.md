Expo Router + React Native；Markdown 使用 `react-native-enriched-markdown`（md4c 原生引擎）配合自研代码块渲染；语法高亮使用 Shiki 并复用 web 的 github-light/github-dark 主题；样式通过 clsx + tailwind-merge 的 `cn()` 工具合并；WebSocket 实时通信通过 `@/data/realtime/ws-client` 抽象。