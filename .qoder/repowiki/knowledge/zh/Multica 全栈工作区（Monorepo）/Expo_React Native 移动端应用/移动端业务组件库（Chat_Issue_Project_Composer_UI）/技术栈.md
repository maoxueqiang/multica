React Native + Expo Router；列表使用 @shopify/flash-list 替代 FlatList；样式基于 NativeWind 与 class-variance-authority；数据层使用 @tanstack/react-query；媒体选择使用 expo-image-picker / expo-document-picker；键盘处理使用 react-native-keyboard-controller。