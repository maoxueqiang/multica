顶层 `make` 默认打印帮助；常用命令包括 `make setup`（安装依赖+确保DB+迁移）、`make start`/`make stop`、`make check`（typecheck + TS 测试 + Go 测试 + Playwright E2E）、`make selfhost`/`selfhost-build`/`selfhost-stop` 以及 `make worktree-env` 为 worktree 生成独立 .env.worktree。