React + TypeScript；UI 基座使用 @base-ui/react 与 shadcn (base-nova 风格)，样式基于 Tailwind CSS 并通过 tailwind-merge + clsx 实现类名合并；Markdown 渲染采用 react-markdown + remark-gfm/rehype-katex/rehype-sanitize/shiki 管线；主题切换使用 next-themes；图表使用 recharts；数字动效使用 @number-flow/react；图标使用 lucide-react。