- 跨平台差异通过文件名后缀区分（如 _unix.go / _windows.go），同一逻辑在两个文件中分别实现。
- 每个 provider 在 execenv 中以独立文件组织（codex_*.go、hermes_*.go、openclaw_*.go 等），统一通过 ExecEnv 装配接口注入。
- 测试文件与被测文件同名并以 _test.go 结尾，集成测试集中放在 *_integration_test.go 中以便选择性跳过。
- 资源清理采用 prepare/rollback 模式，通过 TaskTemp/GC 在任务失败时回滚临时目录与缓存条目。