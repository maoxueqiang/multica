所有 git 子进程通过 `gitEnv()` 注入 `GIT_TERMINAL_PROMPT=0` 和 `safe.directory=*`（通过 `GIT_CONFIG_COUNT/GIT_CONFIG_KEY_*/GIT_CONFIG_VALUE_*` 追加到现有 `GIT_CONFIG_*` 列表末尾，避免覆盖用户已配置的认证/URL 重写），并将工作目录设为系统临时分区根以避免 cwd 被删除导致命令失败。