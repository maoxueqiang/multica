- 按 provider（Codex/Claude/Hermes/OpenClaw/Cursor/Reasonix/QwenPaw 等）拆分实现文件，并在 execenv 中以同名函数集中装配该 provider 的环境变量与 brief。
- 对平台差异采用文件名后缀区分（如 `*_unix.go` / `*_windows.go`），在公共接口下提供 OS 特定实现。
- 需要原子写入或回滚的资源（技能缓存、工作目录、sidecar 进程）统一通过 prepare/rollback 模式组织，失败时保证不留下半完成状态。
- 长生命周期资源（仓库缓存、进程树、GC 队列）通过独立协程 + channel 驱动，避免阻塞主请求路径。