Go 标准库 `os/exec` + `syscall`；Unix 端使用进程组（`Setpgid` + `Kill(-pid, ...)`），Windows 端使用 Win32 Job Object API（`CreateJobObject`、`AssignProcessToJobObject`、`TerminateJobObject`）并通过 `golang.org/x/sys/windows` 访问；Git 通过命令行子进程调用，配合 `--bare` 克隆与 `git worktree` 创建任务工作区。