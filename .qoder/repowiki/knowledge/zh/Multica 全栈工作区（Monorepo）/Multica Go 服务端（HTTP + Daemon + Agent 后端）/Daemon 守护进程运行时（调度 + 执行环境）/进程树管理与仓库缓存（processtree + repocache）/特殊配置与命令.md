测试覆盖包含 `run_unix_test.go`、`cache_test.go`、`cache_windows_test.go` 与 `partial_clone_test.go`，其中 Windows 专用测试文件通过 build tag 隔离；Git 子进程环境强制注入 `GIT_TERMINAL_PROMPT=0` 与 `safe.directory=*`，因此测试需在无 TTY 且允许信任任意目录的环境中运行。