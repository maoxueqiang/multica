新增 provider 需同时完成三处：在 `agent.go` 的 `SupportedTypes` 列表添加字符串、在 `New()` switch 分支返回对应 backend 实例、在 `launchHeaders` 映射中添加用户可见的启动骨架；若该 provider 需要阻止 launch prefix 中的协议标志，还需在 `launchPrefixBlockedArgs` 中登记其 blocklist。