Go + net/http；数据库访问通过 sqlc 生成的 db.Queries（jackc/pgx v5 pgtype UUID/Timestamptz/Text）；并发安全使用 PostgreSQL advisory lock (pg_advisory_xact_lock) 与 FOR KEY SHARE 行级锁；事件广播走 internal/protocol.Event*；指标埋点通过 obsmetrics.RecordEvent 同时上报 Metrics 与 Analytics。