通过 `handler.New(queries, txStarter, hub, bus, emailService, store, cfSigner, analyticsClient, cfg, daemonHubs...)` 一次性装配全部依赖，再由 `cmd/server/router.go` 将单个 Handler 实例挂载到 chi 路由器；各集成（Lark、Slack、DingTalk、WeCom、Telegram、Composio、GitHub PR Snapshot）在 router 层根据环境变量决定是否注入非空客户端。