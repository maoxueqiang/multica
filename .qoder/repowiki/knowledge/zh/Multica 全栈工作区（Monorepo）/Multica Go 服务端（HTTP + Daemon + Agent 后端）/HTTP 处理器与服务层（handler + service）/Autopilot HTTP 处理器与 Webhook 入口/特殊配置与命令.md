Webhook 入口要求外部服务将请求发送至 `/api/webhooks/autopilots/{token}`，其中 token 格式为 `awt_` + 32 字节 URL-safe base64；GitHub/GitLab 等 provider 需携带 `X-Hub-Signature-256: sha256=<hex>` 头，generic provider 可通过 `Idempotency-Key` 头做幂等去重。