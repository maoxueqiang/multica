搜索超时通过 `SET LOCAL statement_timeout = 3000` 在事务内生效，当 pg_bigm/pg_trgm 扩展缺失时会触发 SQLSTATE 57014，被映射为 HTTP 503；心跳批量调度器默认 30 秒 tick（`DefaultHeartbeatBatchInterval`），需与 `RuntimeClaimFreshnessSeconds` 配合保持 `flushInterval + heartbeatInterval + tickInterval < staleThresholdSeconds`。