VCS 集成需设置 `MULTICA_VCS_SECRET_KEY` 才可用；GitHub 需要 `GITHUB_APP_SLUG`+`GITHUB_WEBHOOK_SECRET`（可选 `GITHUB_APP_ID`+`GITHUB_APP_PRIVATE_KEY` 以支持仓库浏览）；Composio 需配置 `COMPOSIO_API_KEY` 且受 feature flag 控制；Redis 限流默认关闭，需在启动时注入 `NewRedisWebhookRateLimiter`。