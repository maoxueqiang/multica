- HTTP 请求经 chi 路由进入 handler，再由 handler 委托给 service 执行业务逻辑，handler 仅负责参数解析、鉴权与响应封装。
- 数据库访问通过 sqlc 生成的类型化查询，避免手写 SQL。
- 后台任务统一由 internal/scheduler 或 internal/daemon 调度，而非 goroutine 散跑。
- 跨进程通信使用 gRPC/WebSocket（daemonws），不直接共享内存。
- 可观测性通过 internal/metrics 与 internal/logger 统一上报指标与结构化日志。