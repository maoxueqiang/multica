构建时需先运行 sqlc 生成 `server/pkg/db/generated` 与 `server/pkg/db/queries/*.sql` 对应的 Go 代码；LLM、VCS、Lark/Slack/DingTalk/WeCom/Telegram 等集成在 `handler.New` 时根据环境变量（如 `MULTICA_LARK_SECRET_KEY`、`MULTICA_VCS_SECRET_KEY`）按需装配，未配置则返回 503 或降级为 no-op。