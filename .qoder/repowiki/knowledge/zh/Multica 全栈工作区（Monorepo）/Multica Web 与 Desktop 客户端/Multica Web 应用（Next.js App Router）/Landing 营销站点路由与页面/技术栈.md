Next.js App Router（Route Group `(landing)`、ISR `revalidate`、`next/font/google`）、React Server Components + 少量 `'use client'` 组件、自实现 i18n（cookie 持久化 + dictionary factory）、GitHub Releases 镜像 CDN (`mc.ai.caijj.net/releases/desktop/release.json`)。