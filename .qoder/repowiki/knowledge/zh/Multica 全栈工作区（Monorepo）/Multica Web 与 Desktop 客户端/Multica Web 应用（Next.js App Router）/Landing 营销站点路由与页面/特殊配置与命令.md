下载页启用 ISR：`fetchLatestRelease` 在 fetch 上设置 `next: { revalidate: 300 }`，页面级也声明 `export const revalidate = 300`，使发布资源每 5 分钟刷新一次；字体栈通过 CSS 变量 `--font-instrument-serif` 在 `app/custom.css` 中组合 CJK 后备字体，而非在 layout 中直接引用。