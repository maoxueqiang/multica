- 每个路由文件仅作为薄包装：导入 `@multica/views/agents` 中的页面组件并以默认函数导出，不做额外逻辑处理。
- 需要动态参数的路由（`[id]`、`[sessionId]`）声明 `params: Promise<{...}>` 类型并通过 `const { x } = use(params)` 解构后作为 prop 传递给子组件。
- 涉及状态交互的路由页显式添加 `'use client'` 指令，纯展示型路由页则保持服务端渲染。