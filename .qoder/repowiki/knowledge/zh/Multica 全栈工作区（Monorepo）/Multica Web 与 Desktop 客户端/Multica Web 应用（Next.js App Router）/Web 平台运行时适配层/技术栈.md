React 18 + Next.js App Router（`useRouter`/`usePathname`/`useSearchParams`/`Suspense`），使用 `useSyncExternalStore` 订阅 hashchange/popstate 以同步路由片段；依赖浏览器 Navigation API (`window.navigation.canGoBack`) 与 History API (`history.replaceState`)。