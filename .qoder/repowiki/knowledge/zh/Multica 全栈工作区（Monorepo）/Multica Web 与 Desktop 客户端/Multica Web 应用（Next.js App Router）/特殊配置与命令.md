开发时通过 `../../.env` 中的 `REMOTE_API_URL` / `DOCS_URL` / `CORS_ALLOWED_ORIGINS` 控制后端代理与 HMR 来源；生产构建使用 `STANDALONE=true` 输出 standalone 产物；Turbopack 因 fumadocs-mdx 兼容性限制需显式走 webpack（dev/build 脚本已传 `--webpack`）。