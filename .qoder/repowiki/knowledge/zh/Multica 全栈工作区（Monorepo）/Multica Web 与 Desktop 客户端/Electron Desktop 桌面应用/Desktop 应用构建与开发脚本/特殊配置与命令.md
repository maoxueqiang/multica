发布构建需 `APPLE_TEAM_ID` 启用 macOS 签名/公证，否则跳过；可通过 `CSC_IDENTITY_AUTO_DISCOVERY=false` 关闭签名走 ad-hoc 签名；`pnpm package -- --all-platforms` 仅支持在 macOS 宿主上构建全部平台矩阵；`pnpm dev:desktop` 会先运行本脚本链再启动 electron-vite。