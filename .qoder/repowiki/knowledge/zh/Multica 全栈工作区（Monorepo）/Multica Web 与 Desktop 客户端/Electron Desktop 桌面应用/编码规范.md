- 跨进程通信通过 `src/shared` 中定义的 TypeScript 类型与消息通道常量统一建模，主进程与渲染进程均从该层导入而非重复声明
- IPC 调用在渲染端经由 `src/preload` 暴露的安全 API 发起，禁止直接访问 `window.require('electron')`
- 每个实现文件配套同名的 `.test.ts(x)` 测试文件，使用 vitest 运行于 Node/Web 双环境
- 桌面专属能力通过 `src/renderer/src/platform/` 下的桥接模块封装，对上层业务组件透明