React + ReactDOM 18、react-router-dom（memoryRouter）、Zustand（带 persist/middleware）、@tanstack/react-query、motion（动画）、lucide-react 图标、fontsource-variable 字体（Inter/Source Serif 4/Ghost Mono），通过 Electron preload 注入的 `window.desktopAPI`/`window.daemonAPI` 与主进程通信。