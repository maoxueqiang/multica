开发模式下通过 `DESKTOP_APP_SUFFIX` 环境变量给 dev 构建附加 app name/userData 后缀，使同一台机器并行 worktree 的 dev 实例互不干扰；非 Windows 平台启动时调用 `fixPath()` 并追加 `/opt/homebrew/bin`、`~/.local/bin` 等 fallback 目录以保证 bundled CLI 能找到 agent 二进制。