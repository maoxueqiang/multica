- 业务页面按功能域拆分为 features/ 或 pages/ 目录，并在 app/ 路由下以 (auth)、(landing)、[workspaceSlug]/(dashboard) 等路由组进行挂载。
- 平台差异通过 platform/ 下的同名适配文件（navigation、document-title、in-app-history 等）抽象，供 web 与 desktop 渲染进程以相同接口调用。
- 每个源文件均配套 .test.ts(x) 单元测试，使用 vitest 运行，测试文件与源码同目录放置。